[package]
name = "steamroller-native-minifier"
version = "0.1.0"
edition = "2021"
description = "Native Rust minifier for steamroller"

[lib]
crate-type = ["cdylib"]

[dependencies]
# TODO: Add napi and napi-derive for Node.js bindings
# napi = "2"
# napi-derive = "2"
# oxc_minifier = "0.x"
