[package]
name = "steamroller-native-resolver"
version = "0.1.0"
edition = "2021"
description = "Native Rust module resolver for steamroller"

[lib]
crate-type = ["cdylib"]

[dependencies]
# TODO: Add napi and napi-derive for Node.js bindings
# napi = "2"
# napi-derive = "2"
# oxc_resolver = "0.x"
